# DESIGN.md

## 气质与意象
凌晨三点，昏暗卧室里只亮着暖黄小夜灯，一只手抱着宝宝，另一只手单手点一下手机完成记录。整个产品应该是这种"半睡半醒间也能用"的状态——温暖、安静、极简。

## 视觉风格
- 极简扁平风，零装饰，信息优先
- 图标全部使用 1.5px 线条风格 SVG，不用 emoji 和拟物图标
- 间距留白大于分割线，用留白分组而非边框
- 圆点（1.5px）作为列表标记，不是色块或图标

## 配色方案
- 背景：暖白 #FFF9F2
- 卡片/内嵌区：#FFFCF8
- 主色：暖金 #D4A76A
- 主色浅底：#FFF3E6
- 主色文字：#C49556
- 文字：深暖灰 #3D3229
- 次要文字：#8B7E74 → #A89888
- 最淡文字：#BFB3A8 / #C4B8AE / #D8CEC4
- 分割线/边框：#EDE5DC
- 紧急提醒：柔和珊瑚 #E8836B

## 字体排版
- 系统默认中文字体，无需加载外部字体
- 倒计时：text-7xl font-light，轻量不压迫
- 正文：text-sm
- 辅助文字：text-xs
- 数字统一 tabular-nums

## 页面结构
- 顶栏：宝宝昵称 + 房间码（极小，不占空间）
- 核心：距上次喂奶倒计时（视觉中心）
- 统计条：次数 | 奶量（一行）
- 记录人输入：极简内联
- 大按钮："喂奶了"
- 确认弹窗：统一使用底部弹窗规范，时间+记录字段+确认
- 今日记录：极简列表，圆点+奶量+人+时间+删除
- 历史入口：文字按钮

## 同级记录弹窗规范

吃药、辅食、便便、清醒及以后新增的同级记录弹窗，统一使用同一种移动端底部弹窗，不为单个记录类型另设计一套尺寸或交互。

### 容器结构

```tsx
<div className="fixed inset-0 z-50 flex items-end justify-center">
  <div className="w-full max-w-sm rounded-t-2xl p-6 pb-8">
    {/* 标题、表单、确认按钮 */}
  </div>
</div>
```

- 遮罩：`rgba(0, 0, 0, 0.25)`，点击空白区域关闭。
- 面板：暖白 `#FFF9F2`，宽度 `w-full max-w-sm`，顶部圆角 `rounded-t-2xl`，内边距 `p-6 pb-8`，贴屏幕底部对齐。
- 面板使用 `role="dialog"`、`aria-modal="true"` 和 `aria-labelledby`；面板内部点击不能冒泡到遮罩。
- 表单较长时只让字段区域滚动，确认按钮保持在面板底部，不能覆盖字段或被键盘遮挡。

### 标题与按钮

- 标题行：`flex items-center justify-between mb-5`。
- 左侧为 18px 线性 SVG 图标 + `text-base font-medium` 标题；图标使用该记录类型的语义色。
- 关闭按钮使用 `p-1`、18px 线性 SVG，颜色 `#BFB3A8`，不使用额外的大块按钮背景。
- 确认按钮：全宽、`rounded-xl`、`py-4`、`text-lg font-medium`，使用记录类型语义色；按下时 `active:scale-[0.98]`，提交中 `disabled:opacity-50`。

### 焦点与手机键盘

- 打开弹窗时不得给任何输入框设置 `autoFocus`，不得主动调用 `focus()`，不得自动拉起手机键盘。
- 用户主动点击输入框后才允许获得焦点并呼出键盘。
- 不使用会替换这套交互的自动焦点 Drawer 行为；同级记录弹窗默认使用普通 `fixed` 底部面板。
- 关闭后再次打开不得继承上一次输入框焦点。

### 移动端验收

- 至少在 390×844 视口验证：打开弹窗不弹键盘、面板宽度和吃药弹窗一致、字段可见。
- 点击食物/药名等输入框后，键盘出现但输入框仍可见；长表单可以在键盘上方滚动。
- 关闭并重新打开一次，确认没有残留焦点、遮罩或滚动状态。

## 动效与交互
- 按钮 active:scale-[0.97]，轻柔
- 无多余动画

## 设计禁忌
- 不要 emoji 做图标（用线条 SVG）
- 不要拟物/3D 风格
- 不要纯白背景
- 不要蓝色主色
- 不要小按钮
- 不要复杂表单
- 不要粗重分割线（用留白）
